// import React from "react";
// import { shallow } from "enzyme";
// import toJson from "enzyme-to-json";
// import { Expand, Remove, Checkmark } from "./svg";

// it("renders Expand", () => {
//   const wrapper = shallow(<Expand />);

//   expect(toJson(wrapper)).toMatchSnapshot();
// });

// it("renders Remove", () => {
//   const props = {
//     item: {
//       value: "test",
//     },
//   };

//   const wrapper = shallow(<Remove {...props} />);

//   expect(toJson(wrapper)).toMatchSnapshot();
// });

// it("renders Checkmark", () => {
//   const wrapper = shallow(<Checkmark />);

//   expect(toJson(wrapper)).toMatchSnapshot();
// });
